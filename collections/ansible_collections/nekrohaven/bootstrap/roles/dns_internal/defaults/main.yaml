# roles/dns/defaults/main.yaml
---
dns_internal_domain: ""
dns_internal_port: "{{ lookup('env', 'DNS_PORT') }}"
# This is the top-level authoritative DNS (usually the router)
dns_internal_authoritative_ipv4: ""
dns_internal_host_fqdn: "{{ ansible_hostname }}.{{ dns_internal_domain }}"
